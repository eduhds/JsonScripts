{
  "$schema": "https://eduhds.github.io/JsonScripts/schema.json",
  "version": 1,
  "variables": {
    "name": "JsonScripts"
  },
  "scripts": {
    "hello": "echo 'Hello World!'",
    "hello-name": "echo 'Hello {{name}}'",
    "ls": "ls -la",
    "ls-home": "ls -la ~",
    "echo": "echo",
    "self": "jnss",
    "build": "bash scripts/build.sh",
    "release": "gh release create {{tag}} -t {{tag}} --generate-notes"
  }
}
